import React from "react"

const ListEdit = () => {
  return (
    <div>
      <form>
        <input type="text" placeholder="{props.value}"/>
      </form>
    </div>
  );
}

export default ListEdit;

